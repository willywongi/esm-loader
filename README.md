ESML - ECMAScript Module Loader
---------

A http/2 server handling ECMAScript Modules to the browser. Run `npm install` to install dependencies, `npm run:build` to create certificates and keys, `npm run` to start server.